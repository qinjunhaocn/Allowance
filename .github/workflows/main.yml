name: Flutter CI

on:
  workflow_dispatch:
    inputs:
      flutterVersion:
        description: 'Flutter SDK version'
        required: false
        default: '3.19.6'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Set up Git
        run: |
          git config --global user.name 'github-actions'
          git config --global user.email 'github-actions@github.com'

      - name: Clone Repository
        run: |
          git clone https://github.com/qinjunhaocn/Allowance.git
          cd Allowance

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          flutter-version: ${{ github.event.inputs.flutterVersion }}

      - name: Install Dependencies
        run: |
          flutter pub get
          flutter doctor

      - name: Build APK
        run: flutter build apk --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: App APK
          path: Allowance/build/app/outputs/flutter-apk/app-release.apk
